services:
  frontend:
    build: .
    container_name: placement-container
    ports:
      - "5000:80"
    restart: always

  backend:
    build: ./backend
    container_name: backend-container
    ports:
      - "3001:3001"
    restart: always